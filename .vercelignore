# Exclude server-side source from static landing deployment on Vercel
# Prevents Vercel from creating Serverless Functions from /api/**
/# Only ignore the landing project's top-level api directory
/api/

# Optional: exclude other server-only code not needed for the landing static build
/# Anchor the rest to the repo root as well
/server/
/backend/
/python-clients/
/__pycache__/
